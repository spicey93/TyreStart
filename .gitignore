# Python bytecode / caches
__pycache__/
*.py[cod]

# Local SQLite database (live data + backups) — keep out of version control
app.db
app.db.bak*

# Bulk product-import seed data (large file) — store/back up separately
products.csv

# Supabase / database connection secrets — local only, never commit
db_config.json
